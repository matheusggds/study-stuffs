describe('Main', () => {]
  describe('Method A', () => {
    context('Case 1', () => {
      it('should happen blabla', () => {
        // Espera que aconteça
        // Entrada de dados / método sum(2,2)
        // Espera retornar (4) => true OR (3) => false => broken test
      })
    })
  })

  describe('Method B', () => {

  })
})
